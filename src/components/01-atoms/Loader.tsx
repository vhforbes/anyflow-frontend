const Loader = () => {
  return (
    <div className="flex items-center w-full h-full flex-grow absolute justify-center bg-base-300 bg-opacity-80">
      <div className="">Loading...</div>
    </div>
  );
};

export default Loader;
